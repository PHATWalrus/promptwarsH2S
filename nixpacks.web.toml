[phases.setup]
nixPkgs = ["bun", "openssl"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun --filter @lexguard/web run build"]

[start]
cmd = "bun --filter @lexguard/web run start"
